framework:
    secret:          ChangeMeOrYouWillFired!
    router:          { resource: "%kernel.root_dir%/config/routing.yml" }
    form:            false
    csrf_protection: false
    session:         ~
    default_locale:  en
    translator:      { fallback: en }
    profiler:        { only_exceptions: false }

larapulse_sitemap:
    base_host:       http://localhost